#include<stdio.h>
int main()
{
	int n,i;
	double num,sum=0;
	printf("enter the valuve of n:");
	scanf("%d",&n);
	for(i=0;i<=n;i++)
	{
		printf("enter a number:");
		scanf("%lf",&num);
		sum+=pow(num,3);
	}
	printf("the sum of hte cubes is:%lf\n",sum);
	return 0;
}
